mutation UpdateNote($input: NoteInput!, $id: Int!) {
  updateNote(input: $input, id: $id) {
    note {
      id
      isPublic
      creatorId
      title
      text
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
